# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Grove working data & datasets (regenerated by the pipeline)
data/work/
data/dataset/
data/raw/*
!data/raw/.gitkeep

# Model weights downloaded on first use
*.pt
*.pth
weights/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local config (config.example.yaml is tracked; user copies it to config.yaml)
config.yaml

# Local demonstration run (CC-licensed sample images + generated artifacts; not shipped)
examples/demo_run/

# Tree-detection demo run + generated boxed preview (regenerable)
data/tree_demo/
mandarin_tree_1.boxed.png

# Keychain CA bundle exported for pip/HuggingFace TLS on the China proxy link
ca.pem
